# Local/private config goes in the include
[include]
        path = ~/.gitconfig_secrets
        path = ~/.gitconfig_osspecific
[hub]
        protocol = https
[alias]
        co      = checkout
        count   = !git shortlog -sn
[color]
        diff = auto
        status = auto
        branch = auto
        ui = true
[core]
        excludesfile = ~/.gitignore
        editor = nvim
[apply]
        whitespace = nowarn
[mergetool]
        keepBackup = false
[difftool]
        prompt = false
[help]
        autocorrect = 1
[push]
        # See `git help config` (search for push.default)
        # for more information on different options of the below setting.
        #
        # Setting to git 2.0 default to suppress warning message
        default = simple
        autoSetupRemote = true
[credential]
	provider = generic
	helper = store
[init]
        defaultBranch = main
[gui]
	recentrepo = C:/Users/cgd55/python/PortfolioManagement
[credential "helperselector"]
	selected = manager
[http]
	lowSpeedTime = 5
	lowSpeedLimit = 1
	connectTimeout = 5
	proxy = http://cgd55:Ammm1mmm2mmm3mmm@webproxy-de.corp.vattenfall.com:8080
[lfs]
	cache = false
[https]
	proxy = http://cgd55:Ammm1mmm2mmm3mmm@webproxy-de.corp.vattenfall.com:8080
